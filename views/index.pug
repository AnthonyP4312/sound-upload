extends layout

block content
  script.
    jQuery(document).ready(function ($) {
      $("#input-id").fileinput();
    }
  
  h3 put the shit here my dude

  .form.form-inline(ref="uploadForm", action="/upload", method="post", enctype="multipart/form-data")
    input.file(id='input-id' type="file", name="sound")
    input.btn.btn-default(type="submit", value="Upload!")
  
  .container(id='list')
    each sound in sounds
      .col-md-1
        a=sound
